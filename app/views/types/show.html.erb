<!-- Controls for previous and next club pages. -->
<a href="#" id="previous-club">Previous</a>
<a href="#" id="next-club">Next</a>

<h1><%= @type.name %> Clubs</h1>

<% @type.clubs.each do |club| %>
  <h3><%= link_to club.name, club_path(club) %></h3>
  <p><%= club.member_number %> members.</p><br>
<% end %>