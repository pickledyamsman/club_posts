<%= form_for @post, action: posts_path, remote: true, method: 'patch' do |f| %>
  <h6>Title:</h6>
  <%= f.text_field :title,  placeholder: "Add a title." %><br>
  <h6>Content:</h6>
  <%= f.text_area :content, placeholder: "Add some content." %>
  <%= f.submit %>
<% end %>