<!-- <% posts.each do |post| %>
  <div>
    <div>
      <strong><%= link_to post.user.email, user_path(post.user) %></strong> 
      Club: <%= link_to post.club, club_path(post.club) %>
    </div>
  
    <div>
      <p><%= post.content %></p>
      <p><%= link_to "edit", edit_post_path(post) if can? :update, post %>
      <%= link_to "delete", post_path(post), method: "DELETE" if can? :destroy, post %></p>
    </div>
  </div>
<% end %> -->