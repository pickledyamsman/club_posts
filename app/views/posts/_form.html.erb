<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@club.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= fields_for :posts do |p| %>
      <%= p.label :title %>
      <%= p.text_field :title, placeholder: "Add a title." %>
      <h5>Content:</h5>
      <%= p.text_area :body, placeholder: "Add some content." %>
    <% end %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>