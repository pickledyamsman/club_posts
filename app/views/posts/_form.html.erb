<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@club.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title, placeholder: "Add a title." %>
    <h5>Content:</h5>
    <%= f.text_area :content, placeholder: "Add some content." %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>